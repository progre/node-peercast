import PcpHub = require('./pcphub');
import PcpSocket = require('./pcpsocket');

class PeerCast {
    constructor() {
        console.log(PcpHub);
        console.log(PcpSocket);
    }
}

export = PeerCast;
